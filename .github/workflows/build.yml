name: Build Picasso Kernel

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-22.04
    steps:
      - name: Setup Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf imagemagick lib32ncurses5-dev lib32readline-dev lib32z1-dev liblz4-tool libncurses5-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev

      - name: Clone Kernel Source
        run: |
          git clone --depth=1 https://github.com/EndCredits/kernel_xiaomi_sm7250.git -b android-4.19 kernel-source

      - name: Setup KernelSU
        run: |
          cd kernel-source
          
          # Dọn dẹp symlink hoặc thư mục rác từ source gốc (nếu có) để tránh xung đột
          rm -rf drivers/kernelsu KernelSU
          
          # Tải và chạy script cài đặt chính thức của KernelSU
          curl -ls https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh | bash -

      - name: Clone Toolchain (Proton Clang)
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git toolchain

      - name: Build Kernel with Docker/DroidSpace Configs
        run: |
          cd kernel-source
          export PATH="$GITHUB_WORKSPACE/toolchain/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CC=clang
          export LLVM=1
          export LLVM_IAS=1
          
          # Áp dụng file config mặc định của picasso
          make O=out vendor/picasso_user_defconfig
          
          # Inject các config bắt buộc cho Docker và DroidSpace OSS
          cat <<EOF >> out/.config
          # Cấu hình bắt buộc cho IPC Namespace
          CONFIG_SYSVIPC=y
          CONFIG_SYSVIPC_SYSCTL=y
          CONFIG_POSIX_MQUEUE=y
          
          # Cấu hình Namespaces đầy đủ
          CONFIG_NAMESPACES=y
          CONFIG_NET_NS=y
          CONFIG_PID_NS=y
          CONFIG_IPC_NS=y
          CONFIG_UTS_NS=y
          CONFIG_USER_NS=y
          
          # Cấu hình Devtmpfs cho Hardware Access
          CONFIG_DEVTMPFS=y
          CONFIG_DEVTMPFS_MOUNT=y
          
          # Cgroups và Mạng
          CONFIG_CGROUPS=y
          CONFIG_CGROUP_CPUACCT=y
          CONFIG_CGROUP_DEVICE=y
          CONFIG_CGROUP_FREEZER=y
          CONFIG_CGROUP_SCHED=y
          CONFIG_CPUSETS=y
          CONFIG_MEMCG=y
          CONFIG_VETH=y
          CONFIG_BRIDGE=y
          CONFIG_OVERLAY_FS=y
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
          CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
          EOF
          
          # Cập nhật lại config để kernel xử lý các dependency liên quan
          make O=out ARCH=arm64 olddefconfig
          
          # Tiến hành biên dịch kernel (Tận dụng tối đa luồng CPU)
          make O=out ARCH=arm64 -j$(nproc --all)

      - name: Pack Kernel with AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git AnyKernel3
          
          # Copy Image hoặc Image.gz vào thư mục AnyKernel3
          if [ -f kernel-source/out/arch/arm64/boot/Image.gz ]; then
            cp kernel-source/out/arch/arm64/boot/Image.gz AnyKernel3/
          else
            cp kernel-source/out/arch/arm64/boot/Image AnyKernel3/
          fi
          
          # Ghi đè file anykernel.sh chuẩn cho Redmi K30 5G (Snapdragon 765G)
          cat << 'EOF' > AnyKernel3/anykernel.sh
          # AnyKernel3 Setup
          # begin properties
          properties() { '
          kernel.string=Picasso Docker Kernel
          do.devicecheck=1
          do.modules=0
          do.systemless=1
          do.cleanup=1
          do.cleanuntil=50
          os.patchlevel=auto
          device.name1=picasso
          device.name2=k305g
          device.name3=
          device.name4=
          device.name5=
          supported.versions=
          supported.patchlevels=
          '; } # end properties

          # shell variables
          block=/dev/block/bootdevice/by-name/boot;
          is_slot_device=0;
          ramdisk_compression=auto;
          patch_vbmeta_flag=0;

          ## AnyKernel methods (standard header)
          . tools/ak3-core.sh;

          ## AnyKernel install
          split_boot;
          flash_boot;
          ## end install
          EOF
          
          cd AnyKernel3
          # Nén thành file ZIP có thể flash qua Recovery
          zip -r9 ../Picasso-Docker-Kernel.zip *

      - name: Upload Kernel Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Picasso-Docker-Kernel
          path: Picasso-Docker-Kernel.zip
